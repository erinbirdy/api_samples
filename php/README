The "categories.php" example shows how to use the REST XML interface to pull a list of top-level categories from your account and list them. 

It also shows some hints on other options you may need to fully utilize MerchantOS' API via PHP's cURL library.